//= require slick-carousel/slick/slick.js

$(function(){

  // banner
  $('.slick').slick({
    autoplay: true,
    fade: true,
    arrows: false,
    dots: true,             // 当前幻灯片指标点
    // dotsClass: 'slick-dots',  // 指标点容器

  });
})
