$('.ui.modal .header').html('新增合作企业');
$('.ui.modal .content').html("<%= j (render partial: 'form', locals: { contact: @contact, remote: true }) %>");
$('.city-group').china_city();
$('.ui.dropdown').dropdown();
$('.popmessage').popup();
$('.ui.modal').modal('show');
